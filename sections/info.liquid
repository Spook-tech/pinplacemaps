<section class="info">
  <div class="container">
    <div class="reported">
      <h2>Women in our research reported noticeable hair growth within 90 days</h2>
      <ul class="reported__list">
        {% for block in section.blocks %}
          {% if block.type == 'block' %}
            <li>
              <div class="top">
                {%- if block.settings.icon != blank -%}
                  {{ block.settings.icon | image_url: height: 70 | image_tag }}
                {%- endif -%}
                <h5>{{ block.settings.title }}</h5>
              </div>
              <div class="bottom">
                {{ block.settings.text }}
              </div>
            </li>
          {% endif %}
        {% endfor %}
      </ul>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Info section",
  "class": "info_section",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Women in our research reported noticeable hair growth within 90 days"
    }
  ],
  "blocks": [
    {
      "type": "block",
      "name": "Block",
      "settings": [
        {
          "type": "image_picker",
          "id": "icon",
          "label": "Icon"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Some text"
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "Text",
          "default": "<p>Some text</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Info section",
      "blocks": [
        {
          "type": "block"
        },
        {
          "type": "block"
        },
        {
          "type": "block"
        }
      ]
    }
  ]
}
{% endschema %}
